var sum3 = function (x, y) {
  // xác định kiểu dữ liệu của biến đầu vào
  return x + y;
};
console.log("check sum 3 =", sum3(10, 5));
// nếu đầu params ko đúng kiểu dữ liệu của argument nó sẽ báo lối
